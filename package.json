{
  "name": "2d-platformer",
  "version": "0.1.0",
  "description": "A Phaser 3 2D platformer scaffold, ready to deploy to Cloudflare Pages.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}
