# Working agreements

## Git / releasing
- **Active development branch is `Stephen-Dev-branch`.** Do work there, not on a
  per-session `claude/*` branch.
- **Always land changes on `main` via a pull request — never push directly to `main`.**
  Develop on `Stephen-Dev-branch`, push it, then open a PR targeting `main`.
