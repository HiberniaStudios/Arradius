# Working agreements

## Git / releasing
- **Always land changes on `main` via a pull request — never push directly to `main`.**
  Develop on the feature branch, push it, then open a PR targeting `main`.
